[project]
name = "acme-core"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ut.tasks]
test = "echo acme-core >> ../../order.log && pytest -q"
lint = "python -c 'import acme_core'"
